import styled from 'styled-components'

export const DropContainer = styled.div.attrs({
    className: 'dropzone'
})`
    line-height: 70px;
    text-align: center;
    font-size: 1.2em;
    border-radius: 5px;
    color: darkgray;
    border: 1.9px dashed blue;
    cursor: pointer;
    font-family: 'Nunito', Helvetica, sans-serif;
`
